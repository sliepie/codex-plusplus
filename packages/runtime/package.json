{
  "name": "@codex-plusplus/runtime",
  "version": "0.1.7",
  "type": "commonjs",
  "main": "dist/main.js",
  "description": "In-app runtime: discovers tweaks, injects settings UI, and reloads on explicit requests.",
  "scripts": {
    "build": "tsc -p tsconfig.json && node ./scripts/bundle-renderer.mjs"
  },
  "dependencies": {
    "@codex-plusplus/sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.19.17",
    "electron": "^41.3.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
